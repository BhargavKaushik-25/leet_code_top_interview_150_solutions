        int totalWater = 0;
        for (int i = 0; i < n; i++) {
            int waterLevel = Math.min(leftMax[i], rightMax[i]);
            int water = waterLevel - height[i];
            if (water > 0) {
                totalWater += water;
            }
        }

        return totalWater;
    }
