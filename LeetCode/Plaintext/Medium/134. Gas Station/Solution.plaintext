            totalTank += diff;
            tank +=diff;

            int diff = gas[i] - cost[i];
        for( int i = 0;i <gas.length ; i++){

        int start = 0;
        int tank  = 0;
        int totalTank = 0;
    public int canCompleteCircuit(int[] gas, int[] cost) {
class Solution {
