        int count = 0 ;
        for(int i = 0; i < nums.length; i++){
            count++;
            m = Math.max(m, i+ nums[i]);

            if(m==nums.length-1){
                break;
            }
        }
    }
        return count;
